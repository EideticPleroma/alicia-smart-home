# PostgreSQL Configuration
POSTGRES_PASSWORD=Kenton235813!
POSTGRES_DB=alicia_db
POSTGRES_USER=alicia_user

# Database Connection
DB_HOST=localhost
DB_PORT=5432

# Optional: Database URL for applications
DATABASE_URL=postgresql://alicia_user:Kenton235813!@localhost:5432/alicia_db

# Application Configuration
NODE_ENV=development

# Optional: Vector dimensions (adjust based on your embedding model)
# Common values: 768 (BERT), 1536 (OpenAI text-embedding-ada-002), 4096 (OpenAI text-embedding-3-large)
VECTOR_DIMENSION=768
